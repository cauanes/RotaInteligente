# ── Frontend ─────────────────────────────────────────────
# O mapa usa MapLibre GL + tiles gratuitos (CARTO/OSM) — sem token necessário.

# URL do backend (em dev o Vite proxy já redireciona /api → backend)
# Em produção, substitua pelo endereço real do backend.
VITE_API_URL=http://localhost:8000
